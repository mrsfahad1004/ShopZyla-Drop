# ShopZyla Drop AI Auto-Lister

This app automatically fetches products from ShopZyla API and lists them to your store (Shopify or ShopZyla).

## How to Run

1. Add your API keys to `config/config.py`.
2. Install dependencies:
